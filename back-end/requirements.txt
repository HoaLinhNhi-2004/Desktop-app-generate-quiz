# Core
flask
flask-cors
flask-sqlalchemy
python-dotenv
cryptography  # AES-GCM at-rest encryption for Gemini API keys

# Image processing
pillow
numpy

# PDF processing
pdfplumber
pdf2image
PyMuPDF  # text block bounding boxes for heatmap overlay

# LLM
requests
google-generativeai  # optional: only if using Gemini provider
google-genai  # used to verify an API key before it enters the rotation pool

# Vector database
chromadb

# YouTube transcript
youtube-transcript-api>=0.6
yt-dlp  # fallback for IP-block bypass
